import { TDeviceType } from "../types/deviceType";

export const CONFIG = {
}

export const BREAKPOINT = {
    MOBILE: 768,
    TABLET: 1024
}

export const DEVICE_TYPE: { [key: string]: TDeviceType } = {
    MOBILE: "mobile",
    TABLET: "tablet",
    DESKTOP: "desktop"
}